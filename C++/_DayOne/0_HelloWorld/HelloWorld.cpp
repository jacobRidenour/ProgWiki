#include <iostream> //<> = in standard library

int main() {
    std::cout << "Hello World" << std::endl; //cout = character out [to the screen]
    return 0; //not strictly necessary but good to have
}

// << write data to a stream
// >> read data from a stream